apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cert
  namespace: homepage
spec:
  secretName: production-tls-secret
  dnsNames:
    - aarnihalinen.fi
    - www.aarnihalinen.fi
  issuerRef:
    name: cert-issuer
    kind: ClusterIssuer
    group: cert-manager.io
